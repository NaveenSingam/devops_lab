apiVersion: v1
kind: Service
metadata:
    name: my-service 
spec:
    ports:
        targetPort: 3001
        port: 8000
        nodeport: 3200
    selector:
        app: my-serv 
    type: LoadBalancer